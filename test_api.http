### 1. 测试注册接口 (放行接口)
# 期望结果: 成功到达Controller，返回 200 OK（或者后端业务特定的响应格式），绝不应该是 401/403
POST http://localhost:8080/api/users
Content-Type: application/json

{
  "username": "testUser",
  "password": "testPassword"
}

###

### 2. 测试登录接口 (放行接口)
# 期望结果: 成功到达Controller，返回 200 OK（或者业务报错如密码错误），绝不应该是 401/403
POST http://localhost:8080/api/users/login
Content-Type: application/json

{
  "username": "testUser",
  "password": "testPassword"
}

###

### 3. 测试其他接口 (未放行接口 - 例如获取用户信息)
# 期望结果: 被 Spring Security 拦截，返回 403 Forbidden
GET http://localhost:8080/api/users/1
Accept: application/json
